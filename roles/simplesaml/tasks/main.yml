---
- name: Install PHP and required modules
  yum:
    name:
      - php
      - php-mbstring
      - php-xml
      - mod_php
    state: latest

- name: Create SimpleSAMLphp directory in /tmp and delete its contents if it exists
  file:
    path: /tmp/simplesamlphp/
    state: directory
    mode: '0755'
    recurse: yes
    # This option will remove all files and directories inside /tmp/simplesamlphp/ before creating it.

- name: Download SimpleSAMLphp to /tmp dir
  get_url:
    url: "{{ simplesaml_url }}"
    dest: /tmp/simplesamlphp/
    mode: '0755'

- name: Extract SimpleSAMLphp
  unarchive:
    src: "{{ lookup('fileglob', '/tmp/simplesamlphp/*.tar.gz') }}"
    dest: /var/www/
    remote_src: true
    extra_opts: [--strip-components=1]
  become: true

- name: Create config, metadata, and cert directories for SimpleSAMLPHP
  become: yes
  file:
    path: /etc/xdmod/simplesamlphp/{{ item }}
    state: directory
    mode: '0755'
  loop:
    - config
    - metadata
    - cert

- name: Install python3 and pip3
  yum:
    name:
      - python3
      - python3-pip

- name: Install require package
  pip:
    name: boto3
    extra_args: "--extra-index-url https://pypi.python.org/simple"
    executable: "/usr/bin/pip3"

- name: create a shib directory in /tmp
  file:
    path: /tmp/shib
    state: directory

- name: Download shibboleth tar from S3
  aws_s3:
    mode: get
    s3_url: "{{ s3_endpoint }}"
    bucket: "{{ s3_shibboleth_bucket_name }}"
    object: "{{ s3_shibboleth_object_name }}"
    dest: "/tmp/{{ s3_shibboleth_object_name }}"
    aws_access_key: "{{ lts_access_key }}"
    aws_secret_key: "{{ lts_secret_key }}"
  vars:
    ansible_python_interpreter: /usr/bin/python3

- name: Extract shib_conf.tar.gz into /etc/
  unarchive:
    src: "/tmp/{{ s3_shibboleth_object_name }}"
    dest: /
    remote_src: yes

- name: Recursively change ownership of a /etc/shibboleth to root
  ansible.builtin.file:
    path: /etc/shibboleth
    state: directory
    recurse: yes
    owner: root
    group: root
  when: uab_idp

- name: Copy all the cert and metadata files to appropriate destinations
  become: yes
  copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: '0644'
  loop:
    - { src: "/etc/shibboleth/uabgrid-incommon-key.pem", dest: "/etc/xdmod/simplesamlphp/cert/uabgrid-incommon-key.pem" }
    - { src: "/etc/shibboleth/uabgrid-incommon-cert-2048-20yr.pem", dest: "/etc/xdmod/simplesamlphp/cert/uabgrid-incommon-cert-2048-20yr.pem" }
    - { src: "authsources_uab_idp.php", dest: "/etc/xdmod/simplesamlphp/config/authsources.php" }
    - { src: "saml20-idp-remote_uab_idp.php", dest: "/etc/xdmod/simplesamlphp/metadata/saml20-idp-remote.php" }
  when: uab_idp

- name: Copy config file from templates to destination
  template:
    src: config.php
    dest: /etc/xdmod/simplesamlphp/config/config.php
    mode: 0644
  when: uab_idp

- name: Copy all the cert and metadata files to appropriate destinations
  become: yes
  copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: '0644'
  loop:
    - { src: "/etc/shibboleth/saml.pem", dest: "/etc/xdmod/simplesamlphp/cert/saml.pem" }
    - { src: "/etc/shibboleth/saml.crt", dest: "/etc/xdmod/simplesamlphp/cert/saml.crt" }
    - { src: "authsources_samltest_idp.php", dest: "/etc/xdmod/simplesamlphp/config/authsources.php" }
    - { src: "saml20-idp-remote_samltest_idp.php", dest: "/etc/xdmod/simplesamlphp/metadata/saml20-idp-remote.php" }
  when: samltest_idp

- name: Copy config file from templates to destination
  template:
    src: config.php
    dest: /etc/xdmod/simplesamlphp/config/config.php
    mode: 0644
  when: samltest_idp

- name: Add [authentication] section to portal_settings.ini
  blockinfile:
    path: /etc/xdmod/portal_settings.ini
    block: |
      [authentication]
      source = "default-sp"
    marker: "# {mark} ANSIBLE MANAGED BLOCK - DO NOT EDIT"
    insertafter: '^basic_auth = "on"$'

- name: Create log directory
  file:
    path: "/var/log/simplesaml"
    state: directory
    mode: '0775'
    owner: apache
    group: apache

- name: Create log file
  file:
    path: "/var/log/simplesaml/simplesamlphp.log"
    state: touch
    mode: '0664'
    owner: apache
    group: apache

- name: Restart httpd service
  become: true
  systemd:
    name: httpd.service
    state: restarted

